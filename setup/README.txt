The order of which these scripts should be run:

1. CreateTables.sql (Creates tables and views)
2. PopulateTables.sql (Populates tables with data)
3. AddTriggers.sql (Creates triggers to record events on new data being added/manipulated)
